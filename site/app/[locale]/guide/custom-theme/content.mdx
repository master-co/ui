This guide walks you through how to use Master CSS variables and styles to implement common specifications in design systems.

By presetting these variables and styles, the complexity of template markup is significantly reduced, making a single source of truth easier to maintain.

---

## Customize the primary color
By default, Master UI uses **indigo** from the [default palette](https://rc.css.master.co/reference/colors) as the primary color. On this site, we use classic amber and yellow.
<ColorPalette filterColors={['amber', 'yellow']} />
```js name=master.css.js
import { variables } from '@master/css'

/** @type {import('@master/css').Config} */
export default {
    variables: {
         primary: variables.yellow,
         text: {
            primary: '$(text-yellow)'
         }
    }
}
```
<details open>
    <summary>View custom theme variables for current site</summary>
    <div>
        <Code lang="js">
            {JSON.stringify(require('~/site/master.css.ts').default.variables, null, 4)}
        </Code>
    </div>
</details>