import { selectSizes } from '@master/ui'

### `size`
<DocProp defaultValue="'md'" types={['string', ...Object.keys(selectSizes).map(x => `'${x}'`)]} />

### `indicator`
<DocProp defaultValue="<SelectIndicator />" types={['JSX.Element']} />

### `outlined`
<DocProp types={['boolean']} />

### `disabled`
<DocProp types={['boolean']} />