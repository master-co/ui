import { buttonSizes, buttonColors } from '@master/ui'

### `size`
<DocProp defaultValue="'md'" types={['string', ...Object.keys(buttonSizes).map(x => `'${x}'`)]} />

### `color`
<DocProp defaultValue="'primary'" types={['string', ...Object.keys(buttonColors).map(x => `'${x}'`)]} />

### `rounded`
<DocProp types={['boolean']} />