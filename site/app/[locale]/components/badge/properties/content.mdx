import { BADGE_SIZES, BADGE_COLORS } from '@master/ui'

### `size`
<DocProp defaultValue="'md'" types={['string', ...Object.keys(BADGE_SIZES).map(x => `'${x}'`)]} />

### `color`
<DocProp defaultValue="'primary'" types={['string', ...Object.keys(BADGE_COLORS).map(x => `'${x}'`)]} />

### `rounded`
<DocProp types={['boolean']} />