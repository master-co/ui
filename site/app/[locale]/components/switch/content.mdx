## Default
<CodePreview src="switch/default"
    raw={require('!!raw-loader!../../previews/switch/default/content').default}
    children={require('../../previews/switch/default/content').default()} />

## Disabled
<CodePreview src="switch/disabled"
    raw={require('!!raw-loader!../../previews/switch/disabled/content').default}
    children={require('../../previews/switch/disabled/content').default()} />

## Arbitrary size
<CodePreview src="switch/arbitrary-size"
    raw={require('!!raw-loader!../../previews/switch/arbitrary-size/content').default}
    children={require('../../previews/switch/arbitrary-size/content').default()} />