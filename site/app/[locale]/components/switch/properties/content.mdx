import { SWITCH_SIZES, SWITCH_COLORS } from '@master/ui'

### `size`
<DocProp defaultValue="'md'" types={['string', ...Object.keys(SWITCH_SIZES).map(x => `'${x}'`)]} />

### `color`
<DocProp defaultValue="'primary'" types={['string', ...Object.keys(SWITCH_COLORS).map(x => `'${x}'`)]} />