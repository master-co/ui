import { switchSizes, switchColors } from '@master/ui'

### `size`
<DocProp defaultValue="'md'" types={['string', ...Object.keys(switchSizes).map(x => `'${x}'`)]} />

### `color`
<DocProp defaultValue="'primary'" types={['string', ...Object.keys(switchColors).map(x => `'${x}'`)]} />