## Normal
<CodePreview src="button/normal"
    raw={require('!!raw-loader!../../previews/button/normal/content').default}
    children={require('../../previews/button/normal/content').default()} />

```tsx
import { Button } from '@master/ui.react'

export default () => <>
    <Button size="xs">Submit</Button>
    <Button size="sm">Submit</Button>
    <Button>Submit</Button>
    <Button size="lg">Submit</Button>
    <Button size="xl">Submit</Button>
</>
```

## Rounded
<CodePreview src="button/rounded"
    raw={require('!!raw-loader!../../previews/button/rounded/content').default}
    children={require('../../previews/button/rounded/content').default()} />

## Round
<CodePreview src="button/round"
    raw={require('!!raw-loader!../../previews/button/round/content').default}
    children={require('../../previews/button/round/content').default()} />

## Square
<CodePreview src="button/square"
    raw={require('!!raw-loader!../../previews/button/square/content').default}
    children={require('../../previews/button/square/content').default()} />