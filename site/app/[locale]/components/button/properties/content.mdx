import { BUTTON_SIZES, BUTTON_COLORS } from '@master/ui'

### `size`
<DocProp defaultValue="'md'" types={['string', ...Object.keys(BUTTON_SIZES).map(x => `'${x}'`)]} />

### `color`
<DocProp defaultValue="'primary'" types={['string', ...Object.keys(BUTTON_COLORS).map(x => `'${x}'`)]} />

### `round`
<DocProp types={['boolean']} />

### `rounded`
<DocProp types={['boolean']} />

### `square`
<DocProp types={['boolean']} />

### `disabled`
<DocProp types={['boolean']} />

### `loading`
<DocProp types={['boolean']} />

### `as`
<DocProp types={['T']} />