import { TOGGLE_SIZES, TOGGLE_COLORS } from '@master/ui'

### `size`
<DocProp defaultValue="'md'" types={['string', ...Object.keys(TOGGLE_SIZES).map(x => `'${x}'`)]} />

### `color`
<DocProp defaultValue="'primary'" types={['string', ...Object.keys(TOGGLE_COLORS).map(x => `'${x}'`)]} />